/* import projects from './projects.json';
import { component$ } from '@builder.io/qwik';
import ProjectItem from './ProjectItem';

export default component$(() => {
  return (
    <div class="flex flex-col gap-4 p-8 bg-gray-800 bg-opacity-30 rounded-lg shadow-2xl mx-auto">
      <header class="text-white text-6xl font-semibold mb-4">Projects:</header>
      <main class="flex flex-wrap m-4 p-4 gap-x-4 gap-y-6 overflow-y-auto max-h-[80vh] max-w-[90vw] lg:max-w-[80vw] styled-scrollbar shadow-2xl">
        {projects.map((project, idx) => (
          <ProjectItem project={project} key={idx} />
        ))}
      </main>
    </div>
  );
});
 */
